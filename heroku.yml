build:
  docker:
    app: app/Dockerfile
run:
  app: (gotenberg --api-port 4000 &) && /bin/bash -c "envsubst '\$PORT' < /tmp/nginx.subst.conf > /tmp/nginx.conf && nginx -c /tmp/nginx.conf -g 'daemon off;'"
